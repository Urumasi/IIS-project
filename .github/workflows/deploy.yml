name: Deploy to prod

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Pull master and restart gunicorn
        run: source .github/scripts/deploy.sh "$DEPLOY_KEY"
        shell: bash
